apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-bird
spec:
  selector:
    app: bird
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
