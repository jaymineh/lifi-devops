apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-birdimage
spec:
  selector:
    app: birdimage
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
